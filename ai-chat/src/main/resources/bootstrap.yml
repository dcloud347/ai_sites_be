spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    url: jdbc:mysql://115.159.84.201:3306/ai?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: ai
    password: liu2138965992
    hikari:
      max-lifetime: 120000
  redis:
    host: 115.159.84.201
    password: liu2138965992
    database: 4
  servlet:
    multipart:
      max-file-size: 10MB        # 设置单个文件最大大小为10MB
      max-request-size: 100MB    # 设置多个文件大小为100MB
  cloud:
    nacos:
      discovery:
        server-addr: http://115.159.84.201:8848
      #        username: nacos
      #        password: nacos
      config:
        server-addr: http://115.159.84.201:8848
    compatibility-verifier:
      enabled: false
  application:
    name: ai-chat

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 8020
  tomcat:
    max-swallow-size: 500MB

